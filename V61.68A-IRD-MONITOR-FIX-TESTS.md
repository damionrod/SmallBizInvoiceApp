# V61.68A IRD Payroll Monitor Link Detection Fix — Test Results

Scope: `supabase/functions/check-payroll-compliance-sources/index.ts` only.

## Fix
- Preserved the existing compliance-monitor workflow and database behaviour.
- Improved official IRD specification discovery to handle relative links, absolute links, HTML-encoded query strings, and IRD media URLs.
- Restricts discovered PDF candidates to the official `ird.govt.nz` domain.
- Added a same-domain canonical IRD specification fallback only when the official landing page still identifies the Payroll Calculations & Business Rules Specification.
- No payroll calculation logic or production payroll-rule write path was added.

## Tests completed
- Current IRD relative PDF-link fixture: PASS.
- HTML `&amp;` query-string fixture: PASS.
- Absolute official IRD PDF URL fixture: PASS.
- Official-page canonical fallback fixture: PASS.
- Non-IRD PDF rejection fixture: PASS.
- Confirmed monitor source contains zero INSERT/UPDATE/DELETE operations against `country_payroll_rules`: PASS.
- Supabase Edge Function deployment/compile: PASS — ACTIVE version 3.
- Package comparison: all original package files are unchanged except the monitor Edge Function; this test note is additive.
- ZIP integrity: PASS.

## External verification
On 15 September 2026 the official IRD landing page exposes the current Payroll Calculations & Business Rules Specification for 1 April 2026 to 31 March 2027. Its current PDF URL uses the stable IRD media path ending in `payroll-calculations-and-business-rules-specification.pdf` with a `modified` query parameter.

## Live invocation limitation
The build container cannot resolve external Supabase hosts, so an authenticated/scheduled HTTP invocation could not be executed from the container after deployment. The Edge Function itself compiled and deployed successfully to Supabase. Use the existing Super Admin **Check for Official Updates** button to perform the live end-to-end source check.
