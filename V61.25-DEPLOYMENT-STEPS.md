# V61.25 Deployment Steps

Deploy the package to Netlify.

## Scope
- Fixes only the New Pay Run tax-code profile compatibility fallback.
- If the stored NZ `paye.tax_codes` row cannot be parsed, uses the same tax-code behaviour already defined in the existing v60.2 country payroll rules migration.
- No tax rates, payroll formulas, database rows, or other modules are changed.

## SQL
No SQL is required.
