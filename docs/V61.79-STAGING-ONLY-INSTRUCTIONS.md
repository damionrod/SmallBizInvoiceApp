# V61.79A — Staging-only execution instructions
No database execution was performed while building this package.

1. Restore a disposable Supabase clone matching the pre-V61.79A schema.
2. Back it up.
3. Review the V61.79A migration and correction report.
4. Apply `20260920162000_v6179a_schedule_dashboard_foundation_correction.sql` to that disposable environment only.
5. Prepare disposable fixture UUIDs for two businesses and owner/admin/staff users plus Business A customer/job/quote/invoice/employee.
6. Run the SQL harness with psql variables, for example:
   psql "$STAGING_DATABASE_URL" -v business_a=... -v business_b=... -v owner_a=... -v admin_a=... -v staff_a=... -v owner_b=... -v customer_a=... -v customer_b=... -v job_a=... -v quote_a=... -v invoice_a=... -v employee_a=... -f tests/v6179a-staging-database-tests.sql
7. Run the review checklist and Supabase advisors.
8. Test repeat migration execution.
9. Test rollback first while a temporary Schedule entitlement exists: it must stop before dropping objects.
10. Remove only the temporary staging Schedule plan/business references, then rerun rollback and verify complete removal.
11. Any failure blocks Phase B and production consideration.


## V61.79B test-harness correction
The psql fixture values are copied to transaction-local `v6179a.*` settings. PL/pgSQL dollar-quoted blocks read them with `current_setting()`. The generated schedule ID is stored the same way. The static suite guards against reintroducing raw psql variables inside dollar-quoted blocks.
