# v61.89B Stock & Equipment preview: staging verification

## Status

Operational register only. Do not enable or deploy to production from these checks alone. No production Supabase project was modified; no production Netlify deploy was made. The application migration was applied to the isolated Supabase project `finlo-v6170b-isolated-test` (`smvvevrtjqkflyqkyape`), first as the supplied baseline migration, then as an additive revision covering the new columns and RPCs. The final ZIP contains the consolidated **single** migration for a fresh application. The test project's migration history ends at v6179f, whereas the live project includes v6187/v6188. Relevant expense, invoice, membership and entitlement columns were compared read-only with production and match, but the staging project is **not a full current-schema clone**. Retest on a current-schema staging branch before production use. Production data was never queried for transaction contents or changed.

## Database results (2026-09-23 UTC)

All fixtures below were executed on the isolated test database in explicit transactions and rolled back. Confirmed after testing: 0 businesses, 0 stock items and the module still globally disabled. These tests use real PostgreSQL rows and authenticated role/JWT contexts, not mocked database calls.

| Flow | Result | Evidence and limits |
| --- | --- | --- |
| Split mixed supplier bill | PASS | 4 bill lines created; stock, supplies and equipment classification created 3 confirmed reviews, retained source bill values |
| Candle stock sold | PASS | Selected invoice JSON line with 2 units; balance became 8; another unit on the same line was rejected |
| Work supplies | PASS | Job ID validated in tenant; used 1 of 4 units |
| Return and stocktake | PASS | 1 unit returned and 1 removed in stocktake; quantity remained 8; both movements recorded |
| Opening equipment atomic | PASS | Invalid GST basis failed and left no item; valid opening created exactly one asset |
| Disposal | PASS | Disposal date and proceeds stored in the asset register; no posted accounting entry |
| Correction | PASS | Untouched stock purchase was reversed with an audit record and review reopened; later stock movement requires accountant review |
| Expense/GST invariant | PASS | Original split bill ex GST plus GST remained $540.50; no GST table written by module. Not a full filed GST regression |
| Bank matching | PARTIAL | No new bank allocation appeared from the module operations; existing bank match UI/RPC was not exercised |
| Two tenants and permissions | PASS | Bookkeeper could add an item; viewer and business without the module were denied; cross-business write denied; business A saw 0 rows belonging to business B |
| Older bills | PASS at SQL level | 65 test bills accessible in pages of 30/30/5; browser search/pagination is not yet automated end to end |
| Same-date tax rule correction | PASS | With Super Admin session, published v1 became retired and v2 was published with the same effective date, history retained |
| Production subscription expiry | NOT TESTED | Isolated project lacks the v6187 cancellation guard; requires current-schema staging |
| Desktop and phone browser flows | NOT TESTED | Playwright is installed but no Chromium browser executable is available in this workspace |

Existing Node regression suite in the working tree: **85 passed, 0 failed** after copying the reviewed files; includes earlier subscription/referral checks and four original static Stock & Equipment contract checks. Updated v61.89B checks are included in `tests/v6189b-stock-equipment-review.test.js`. Test isolation does not prove correctness of a deployed PostgREST build or actual filed reports.

## What changed from supplied v61.89 ZIP

- `supabase/migrations/20260924110000_v6189_stock_equipment_foundation.sql`: atomic opening-equipment RPC, correction RPC with compensating movements and refusal once inventory has moved, explicit invoice-line index and quantity verification, same-date published reference correction with audit history, archived corrected asset support.
- `public/stock-equipment.js`: server-side paged and searchable supplier bills; on-demand invoice/job lookups by human number and explicit invoice-line selection; short context-specific movement form; atomic opening-equipment save; correction action and accurate Tax Rules language.
- `public/stock-equipment.css`: responsive search controls and relevant-field visibility.
- This staging report and the focused v61.89B tests.

Unmodified from supplied v61.89 ZIP: invoice, bank, expense, GST, payroll, subscription, referral and Financials implementation.

## Before any production enablement

1. Create a staging branch from the **current** Supabase main schema (branch provisioning may incur a charge), apply the consolidated migration there and repeat authenticated business/role scenarios including canceled and expired subscriptions. Run migrations on a database clone or staging first, never on live without approval.
2. Run the real browser app with two tenants on phone and desktop. Confirm business switching, source document access, form layout, older-bill search, invoices with various item structures, failed RPC feedback, and Super Admin approval. No browser-based QA was available in this environment.
3. Exercise a genuine existing bank match of a recorded supplier bill before and after classification. Verify expense payment count, payable balance, GST totals, bank allocations and accounting outputs are identical. The module itself does not call bank matching.
4. Review how returns relate to invoice credits, corrected equipment with prior depreciation, purchase grouping, GST registration/private use, stock held at period end, incomplete invoice quantities, concurrency, posted accounting periods and accountant exports. Changed assets and stock after sale correctly require a separate reviewed correction flow.
5. A separate accountant-reviewed accounting integration is still required to reclassify existing supplier expenses once (without duplicate expense/GST), post cost of sales, stock/asset balances and book/tax depreciation, reconcile credit notes and job actuals, then export approved schedules. No automatic tax deduction, depreciation, Investment Boost or balance-sheet posting exists. All register values remain **operational estimates**.
