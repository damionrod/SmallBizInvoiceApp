# V61.79D Phase A Advisor-Hardening — Staging Validation Report

## Scope and project identity
- Approved disposable project: `finlo-v6170b-isolated-test`
- Project reference: `smvvevrtjqkflyqkyape`
- Production reference explicitly excluded: `oxsbzytwbphagcbilxud`
- Runtime files changed: **0**
- Phase B / Dashboard UI / Schedule UI / Google Calendar OAuth: **not started**
- Supabase CLI version / `--help`: unavailable because CLI is not installed; connected Supabase tooling used.

## Migration added
`supabase/migrations/20260920170000_v6179d_phase_a_advisor_hardening.sql`

Migration SHA-256: `7123e7190182e84c58e36b132e7ac9692cacd7fd9f86b8fe6e81cd000ea29d6f`

## Policy corrections
`v6179_google_connections_own_read` now compares `user_id = (select auth.uid())`.

Removed broad `FOR ALL` policies:
- `v6179_schedule_series_write`
- `v6179_schedules_write`
- `v6179_schedule_assignments_write`

Replaced with operation-specific policies while retaining the existing explicit SELECT policies:
- recurrence: `v6179_schedule_series_insert`, `_update`, `_delete`
- schedules: `v6179_schedules_insert`, `_update`, `_delete`
- assignments: `v6179_schedule_assignments_insert`, `_update`, `_delete`

INSERT uses `WITH CHECK`; UPDATE uses `USING` + `WITH CHECK`; DELETE uses `USING`. All call the same `v6179_schedule_role_allowed(business_id,true)` rule as the prior write policy.

## Foreign-key advisor correction
Before V61.79D, the advisor reported 28 unindexed FKs, 19 of which belonged to V61.79 tables. Existing leading-column indexes already covered other V61.79 FKs. V61.79D added 19 indexes only for the V61.79 FKs still reported unindexed:
- `job_recurrence_series_job_costing_id_idx`
- `job_recurrence_series_customer_id_idx`
- `job_recurrence_series_created_by_idx`
- `job_recurrence_series_updated_by_idx`
- `job_schedules_job_costing_id_idx`
- `job_schedules_quote_id_idx`
- `job_schedules_customer_id_idx`
- `job_schedules_invoice_id_idx`
- `job_schedules_recurrence_series_id_idx`
- `job_schedules_created_by_idx`
- `job_schedules_updated_by_idx`
- `job_schedule_assignments_business_id_idx`
- `job_schedule_assignments_created_by_idx`
- `job_schedule_assignments_updated_by_idx`
- `google_calendar_connections_user_id_idx`
- `google_calendar_event_links_employee_id_idx`
- `google_calendar_event_links_user_id_idx`
- `google_calendar_sync_log_connection_id_idx`
- `google_calendar_sync_log_schedule_id_idx`

After V61.79D, the unindexed-FK advisor count fell from **28 to 9**. All 9 remaining findings are baseline tables outside V61.79 scope.

## Complete staging database/RLS harness
Result: **17/17 PASS** after V61.79D.

Passed assertions:
1. admin A can read schedule
2. all six tables RLS enabled
3. authenticated client cannot manufacture Google connection state
4. complete expected V61.79D RLS policy list present
5. cross-business customer reference rejected
6. incomplete time pairs rejected
7. job-costing deletion preserved schedule
8. owner A entitled
9. owner B cannot read Business A schedule
10. owner can read own schedule
11. owner denied when Schedule not entitled
12. quote deletion preserved schedule
13. RLS hides schedule when entitlement removed
14. six tables exist
15. staff A denied schedule read
16. staff A denied schedule write
17. timezone and recurrence validation enforced

Additional authenticated database-context CRUD probe: owner and admin INSERT/UPDATE/DELETE on Schedule all **PASS** with entitlement enabled.

## Deletion compatibility
Owner-created Schedule referencing customer/job/quote/invoice was tested in a rollback transaction. Deleting all four source records preserved the Schedule and set all four references to NULL. **PASS**.

## Repeat migration / idempotence
The complete V61.79 foundation migration followed by V61.79D was executed again against the already-migrated staging schema. **PASS** with no duplicate-object failure.

## Rollback
- Successful rollback with no Schedule plan/business references: **PASS**.
- Foundation + V61.79D reapplication after rollback: **PASS**.
- Protected rollback with one plan reference and one business override: **PASS (correct refusal)**.
- Exact refusal: `V61.79A rollback stopped safely: Schedule is referenced by 1 plan(s) and 1 business override(s). Remove staging-only Schedule entitlements/overrides deliberately, then rerun rollback.`
- After refusal, V61.79 objects remained intact.

## Advisor results: before → after
### Performance
- `auth_rls_initplan`: **1 → 0**. V61.79 Google ownership policy finding cleared.
- `multiple_permissive_policies`: **8 → 5**. All three V61.79 Schedule-table findings cleared; five baseline findings remain.
- `unindexed_foreign_keys`: **28 → 9**. All V61.79 unindexed-FK findings cleared; nine baseline findings remain.
- `unused_index`: expected staging informational findings remain/increased because the staging database has almost no workload. No scheduling indexes were removed based only on this signal.

Remaining baseline unindexed FKs:
- `business_memberships_user_id_fkey`
- `business_modules_module_id_fkey`
- `customers_business_id_fkey`
- `invoices_business_id_fkey`
- `job_costings_business_id_fkey`
- `payroll_employees_business_id_fkey`
- `quotes_business_id_fkey`
- `subscriptions_business_id_fkey`
- `subscriptions_plan_id_fkey`

Remaining baseline multiple-permissive-policy findings:
- `customers`
- `invoices`
- `job_costings`
- `payroll_employees`
- `quotes`

### Security
The generic advisor still reports four authenticated-callable SECURITY DEFINER functions: two pre-existing baseline helpers (`current_business_id`, `v6147_current_business_role`) and the two V61.79 helpers.

The two V61.79 helpers are retained as reviewed exceptions. Database inspection confirmed they:
- reject null `auth.uid()`;
- validate the current active business via `current_business_id()`;
- enforce Schedule entitlement and owner/admin role;
- have explicit `search_path=public`;
- are not executable by `anon`;
- routine privileges list only `authenticated` and `postgres` (no PUBLIC grant).

The advisor also reports leaked-password protection disabled on this disposable project's Auth configuration. This is project-level configuration, not introduced by V61.79D.

## Authenticated HTTP Data API
A genuine authenticated PostgREST request using real disposable user access tokens was **not executed** in this environment. This remains a **pre-production integration test** and is not marked passed.

## Status
All required database/RLS regression tests passed after V61.79D. Phase A status is therefore:

**database staging-approved, with authenticated HTTP Data API validation pending before production**

Production project `oxsbzytwbphagcbilxud` was not accessed or modified during V61.79D validation.
