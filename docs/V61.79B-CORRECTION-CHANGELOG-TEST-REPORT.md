# V61.79B correction changelog and test report

## Scope
Correction-only revision of V61.79A. No migration SQL change. No frontend/runtime change. No database execution.

## Changed files
- tests/v6179a-staging-database-tests.sql
- tests/v6179a-phase-a-migration-static.test.js
- docs/V61.79B-CORRECTION-CHANGELOG-TEST-REPORT.md
- docs/V61.79-STAGING-ONLY-INSTRUCTIONS.md

## Correction
- All supplied psql fixture variables are copied into transaction-local `v6179a.*` settings immediately after `BEGIN`.
- The generated schedule ID is stored in `v6179a.schedule_id`.
- Dollar-quoted `DO $$ ... $$` bodies use `current_setting(... )::uuid`; they contain no raw psql fixture references.
- Executable staging tests now explicitly assert the complete nine-policy V61.79A RLS policy list.
- Static test now fails if any `:'variable'` reference appears inside a dollar-quoted staging-test block.

## Validation
Static tests are executable without a database and are run during packaging.
The SQL staging suite remains authored but unexecuted. It requires a disposable Supabase environment and fixture IDs.
V61.79B is not staging-approved.
