# V61.12 Deployment

Deploy the updated files to Netlify.

No SQL changes are required.

This release only fixes the payslip CSS scope so emailed PDFs use the same table width/layout rules as the on-screen and downloaded payslip. Payroll calculations, data, database structure and other modules are unchanged.
